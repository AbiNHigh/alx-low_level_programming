#include �"main.h"
/**
* print_alphabet – Prints the alphabet in lowercase
*
* Return: Always 0 (Success)
*/
void print_alphabet(void)
{
	char alpha;
	for (alpha = ‘a’;'a'; alpha <= 'z'; alpha++) for (alp
}
_putchar (alpha);
}
_putchar(‘;('\n');
}
